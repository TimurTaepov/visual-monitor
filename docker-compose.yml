services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    command: uvicorn vlm_evals.serving.app:app --host 0.0.0.0 --port 8000

